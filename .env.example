# Copy to .env (gitignored) and fill in.
# `just` loads this automatically (set dotenv-load); the bpg/proxmox provider
# reads these environment variables natively.
# Token creation: docs/runbooks/proxmox-api-token.md
PROXMOX_VE_ENDPOINT=https://10.20.0.50:8006/
PROXMOX_VE_API_TOKEN=tofu@pve!tofu=00000000-0000-0000-0000-000000000000
