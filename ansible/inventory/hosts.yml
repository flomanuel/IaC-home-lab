---
# Static inventory. Must be kept in sync with the tofu/vm_*.tf definitions
all: # Metagroup. Organizes multiple groups.
  children:
    linux:
      children:
        # VMs: Docker Compose
        docker_hosts:
          hosts:
            ubuntu-01:
              ansible_host: 10.50.1.10

        # VMs: bare systemd services
        svc_hosts:
          hosts: {}

#        k3s_cluster:
#          children:
#            k3s_server:
#              hosts: {}
#              # k3s-1:
#              #   ansible_host: 10.50.2.10
#            k3s_agents:
#              hosts: {}

    # Provisioned by tofu, but hand-managed.
    windows:
      hosts: {}
