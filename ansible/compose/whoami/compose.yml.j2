# Example stack. Deploy it by adding to a docker host's host_vars:
#   compose_services: [whoami] # do not forget to add to backup_sources
services:
  whoami:
    image: traefik/whoami:latest
    restart: unless-stopped
    ports:
      - "80:80"
