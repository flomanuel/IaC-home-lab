---
# Needs group_vars/all/secrets.yml
- name: SeaweedFS S3 on TrueNAS LXC host
  hosts: truenas_s3
  become: false
  gather_facts: true
  roles:
    - { role: base, tags: base }
    - { role: nfs_mounts, tags: nfs }
    - { role: seaweedfs, tags: seaweedfs }
